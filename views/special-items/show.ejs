<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("../partials/head.ejs") %>
</head>

<body>
    <%- include("../partials/styles.ejs") %>

        <div class="full-page">
            <header>
                <%- include("../partials/header.ejs") %>
            </header>
            <div class="shell">
                <section>
                    <h3>
                        <%=item.name %>
                    </h3>
                    <p>
                         Type: <%= item.type %><% if(item.atnmt===true){ %>, Requires Attunement <% } %>
                    </p>
                    <p>
                        <%= item.description %>
                    </p>
                    <div class="btn-group">
                        <button class="btn"><a href="/special-items/<%= item._id %>/edit">Edit Item</a></button>
                        <button class="btn"><a href="/special-items">Back</a></button>
                    </div>
                </section>
            </div>
        </div>

</body>

</html>